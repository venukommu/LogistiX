# LogistiX Datasets

This directory contains benchmark logistics datasets, synthetic routing traces, and telemetry schemas for evaluating decision intelligence and recommendation models.

## Structure
- `dispatch/`: Historical dispatch requests and assignment logs.
- `routing/`: Benchmark routing problems (VRP, TSP, time-window constraints).
- `embeddings/`: Pre-embedded logistics domain glossaries and regulation docs.
