<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <parent>
        <groupId>org.logistix</groupId>
        <artifactId>logistix-parent</artifactId>
        <version>0.1.0-SNAPSHOT</version>
        <relativePath>../backend/pom.xml</relativePath>
    </parent>

    <artifactId>logistix-examples</artifactId>
    <name>LogistiX Examples</name>
    <description>Self-contained code samples and developer guides for LogistiX</description>

    <dependencies>
        <dependency>
            <groupId>org.logistix</groupId>
            <artifactId>logistix-dsl</artifactId>
        </dependency>

        <dependency>
            <groupId>org.logistix</groupId>
            <artifactId>logistix-engine</artifactId>
        </dependency>

        <dependency>
            <groupId>org.logistix</groupId>
            <artifactId>logistix-domain</artifactId>
        </dependency>

        <dependency>
            <groupId>org.logistix</groupId>
            <artifactId>logistix-common</artifactId>
        </dependency>

        <dependency>
            <groupId>org.logistix</groupId>
            <artifactId>logistix-model</artifactId>
        </dependency>

        <dependency>
            <groupId>org.logistix</groupId>
            <artifactId>logistix-ai</artifactId>
        </dependency>

        <dependency>
            <groupId>org.logistix</groupId>
            <artifactId>logistix-rag</artifactId>
        </dependency>

        <dependency>
            <groupId>org.logistix</groupId>
            <artifactId>logistix-mcp</artifactId>
        </dependency>

        <dependency>
            <groupId>org.logistix</groupId>
            <artifactId>logistix-simulation</artifactId>
        </dependency>

        <dependency>
            <groupId>org.logistix</groupId>
            <artifactId>logistix-benchmark</artifactId>
        </dependency>

        <dependency>
            <groupId>org.junit.jupiter</groupId>
            <artifactId>junit-jupiter</artifactId>
            <scope>test</scope>
        </dependency>

        <dependency>
            <groupId>org.assertj</groupId>
            <artifactId>assertj-core</artifactId>
            <version>3.27.3</version>
            <scope>test</scope>
        </dependency>

        <dependency>
            <groupId>org.mockito</groupId>
            <artifactId>mockito-core</artifactId>
            <version>5.15.2</version>
            <scope>test</scope>
        </dependency>
    </dependencies>
</project>
